spring:
  application:
   name: firstSampleService  
  profiles:
    active: local
    
eureka:
  instance:
    instance-id: firstSampleService-instance
    prefer-ip-address: true
  client:
    register-with-eureka: true 
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  
myconfig:
  value: jhpark222

management:
  endpoints:
    web:
      exposure:
        include: "*"


